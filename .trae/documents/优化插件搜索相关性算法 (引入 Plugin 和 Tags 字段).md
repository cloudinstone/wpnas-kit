我已分析了 `src/App.js` 中的搜索相关性计算逻辑。目前的评分方案（Name=2, Description=1）较为基础，未能充分利用插件的其他关键信息。

针对您的需求，我制定了以下修改计划：

### 1. 修改 `calculateRelevance` 函数
我将重写该函数以引入 `plugin` (插件ID/路径) 和 `tags` (标签) 字段，并采用以下更合理的加权评分方案（4-3-2-1 方案）：

*   **Name (名称)**: **4分** (原2分)。名称是用户意图的最直接体现，应具有最高优先级。
*   **Plugin/Slug (插件ID)**: **3分** (新增)。用户常搜索特定的插件标识（如 `contact-form-7`），这是非常精准的匹配。
*   **Tags (标签)**: **2分** (新增)。标签是经过分类的关键词，比普通描述更具相关性。
*   **Description (描述)**: **1分** (保持)。描述文本较长，包含的信息较泛，作为基础匹配权重最低。

### 2. 代码实现细节
*   **Plugin 字段**: 使用 `item.plugin` (通常是 `slug/file.php` 格式) 进行匹配。
*   **Tags 字段**: 考虑到 API 返回格式的不确定性，将兼容处理 `tags` 为数组（`['tag1', 'tag2']`）或对象（`{'slug': 'Name'}`）的情况，确保代码健壮性。
*   **权重调整**: 实施上述 4-3-2-1 评分逻辑。

### 3. 预期效果
*   搜索 "seo" 时，名称含 "seo" 的插件将排在最前。
*   搜索特定 slug 或 ID 时，能精准定位到对应插件。
*   通过标签匹配到的插件将比仅在描述中提到的插件排名更靠前。

请确认是否执行此修改方案。