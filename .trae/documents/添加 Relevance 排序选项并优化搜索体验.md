是的，可以添加 "Relevance" 排序选项。目前的实现中，搜索时的排序逻辑被强制覆盖了，这导致无法选择其他排序，也无法显式选择 Relevance。

我们将进行以下修改来实现您的需求：

1.  **添加 Relevance 字段**：
    在数据视图的字段列表中新增一个 "Relevance" 字段。它将基于搜索评分 (`_score`) 进行排序。
    *   该字段默认在表格中隐藏（不会弄乱界面），但在 "Sort By" 下拉菜单中可选。

2.  **优化搜索时的排序逻辑**：
    *   **移除强制覆盖**：不再强制搜索时只能按默认顺序排列，允许用户自由切换排序（例如在搜索结果中按 "Name" 或 "Updated Date" 排序）。
    *   **智能自动切换**：当您开始输入搜索词时，系统会自动将排序切换为 "Relevance"，以确保最相关的结果排在前面。当您清空搜索词时，排序会自动恢复为默认（如按 "Name" 排序）。

这样修改后，您将在 "Sort By" 菜单中看到 "Relevance" 选项，并且拥有完全的排序控制权。